# S3 · 建模迭代 —— Scaling Laws 科研全仿真

> 口径三段式
> - 目标：还原拟合函数形式从弱到强的迭代链。
> - 现状：《档案·S1》+《档案·S1.5》+《档案·S2》+ 论文 §3–§6 的拟合方法细节。
> - 限制：v0/v1 可以是【合理重构】，但每一版的"死因"必须说得让内行信服；凡陈述"某个设计是为了解决某个问题"，必须引出论文里的动机原句，否则降级为待验猜想；不知道最终拟合参数值。

---

这篇论文的"建模迭代"不在算法层，而在**拟合函数形式**层。我们要回答的问题是：给定一组 (N, D, C, S) 和对应的测试损失 L，什么样的函数 L(N, D, ...) 最能描述这些数据，并且能外推到没见过的规模？

以下是三个版本的迭代。

---

## v0：三个独立幂律

**最直接的形式化**：

分别拟合 L(N)、L(D)、L(C)，每个都假设为简单幂律：

L(N) = (Nc/N)^αN
L(D) = (Dc/D)^αD
L(C) = (Cc/C)^αC

这是任何人拿到数据后的第一反应。它简单、只有 two parameters per curve、而且 power law 在 log-log 图上就是直线，容易检查。

**死因**：三个独立的幂律无法回答联合问题。具体来说：
1. 如果同时改变 N 和 D，L 会怎样？三个独立方程无法给出 L(N, D)。
2. 固定 C，如何选择最优的 N 和 D 分配？需要知道 L(N, D) 的形式才能做优化。
3. 过拟合程度如何随 N/D 比值变化？这需要 L(N, D) 的显式形式。

此外，v0 还有一个技术问题：C 的定义不唯一。论文 §2.1 说 C ≈ 6NBS（forward + backward + parameter update），但 batch size B 的选择会影响 C 的值——同一个模型用不同的 B 训练，C 不同但 L 相同。这意味着 L(C) 的幂律只在"固定 B"的条件下成立，不是一个普适关系。

【可查证：§3.3 说 "Note that in these results the batch size B remains fixed for all models, which means that these empirical results are not truly optimal. We will account for this in later sections using an adjusted Cmin to produce cleaner trends."】

---

## v1：L(N, D) 联合拟合

**修补思路**：我们需要一个函数同时依赖 N 和 D，并且在极限情况下退化为 v0 的幂律。

修补思路来自§4.1 的三条原则：

原则 1：词表大小或 tokenization 的变化应导致损失的整体缩放。函数形式必须允许这种缩放。
原则 2：固定 D、令 N→∞ 时，L 应趋近 L(D)；固定 N、令 D→∞ 时，L 应趋近 L(N)。
原则 3：L(N, D) 应在 D=∞ 处解析，即具有 1/D 的整数幂级数展开。

满足这三条的函数形式是：

L(N, D) = [ (Nc/N)^(αN/αD) + Dc/D ]^αD

【可查证：§4.1 明确列出了这三条原则，且说 "We have chosen the parameterization (1.5) using three principles"。方程 (1.5) 即上述形式】

**v1 的改进**：
- 自然退化为 v0：D→∞ 时 L → (Nc/N)^αN；N→∞ 时 L → (Dc/D)^αD
- 过拟合程度 δL = L(N,D)/L(N,∞) − 1 可以解析推导
- 最优数据分配 D ∝ N^(αN/αD) 可以由此推出

**暴露的新问题**：
1. 这个形式在 S（训练步数）维度上完全没做处理。它假设模型已经训练到"对给定 (N, D) 最优"的程度，但实际训练中我们控制的是 S 而非"最优训练程度"。
2. 原则 3（D=∞ 处解析）的论证非常弱——论文自己承认 "Theoretical support for this principle is significantly weaker than for the first two"。如果 1/D 修正不是主导项，这个函数形式可能选错。
3. 拟合质量在小 D 端不好——论文说 "We obtain an excellent fit, with the exception of the runs where the dataset has been reduced by a factor of 1024, to about 2 × 10^7 tokens"。

【可查证：§4.2 说 "We obtain an excellent fit, with the exception of the runs where the dataset has been reduced by a factor of 1024, to about 2 × 10^7 tokens. With such a small dataset, an epoch consists of only 40 parameter updates. Perhaps such a tiny dataset represents a different regime for language modeling."】

---

## v2：L(N, Smin) + Cmin 修正

**最后一块拼图**：把训练步数 S 显式纳入模型，并用 Sam 的临界 batch size 工具修正 C 的定义。

v2 分为两步。

**第一步**：定义 Smin——等效的最小训练步数。大多数模型在 B = 2^19 tokens 下训练，这不是最优 batch size。用 [MKAT18] 的关系式 Smin(S) = S / (1 + Bcrit(L)/B)，把任意 batch size 下的训练步数转换为"等效最优 batch size 下的最小步数"。

【可查证：§5.1 方程 (5.4)：Smin(S) ≡ S / (1 + Bcrit(L)/B)；§5.1 解释了 Bcrit(L) 的定义和来源】

类似地，定义 Cmin(C) = C / (1 + B/Bcrit(L))，把实际算力转换为"等效最优 batch size 下的最小算力"。

【可查证：§5.1 方程 (5.5)：Cmin(C) ≡ C / (1 + B/Bcrit(L))】

**第二步**：拟合 L(N, Smin) = (Nc/N)^αN + (Sc/Smin)^αS。

这个形式的灵感来源：它把"容量不足"和"训练不足"的惩罚加在一起。第一项是模型容量限制（参数不够多），第二项是优化限制（步数不够多）。两项都是幂律，指数不同。

【可查证：§5.2 方程 (1.6)：L(N, Smin) = (Nc/N)^αN + (Sc/Smin)^αS；§5.2 说 "We will fit the stable, Adam-optimized training runs using Equation (1.6)"、"The power-law dependence of the loss on Smin reflects the interplay of optimizer dynamics and the loss landscape."】

**v2 的额外收获**：有了 L(N, Smin) 和 Bcrit(L) 的关系，就可以**从理论上推导**最优分配 N(Cmin)、B(Cmin)、S(Cmin)、D(Cmin)，而不仅仅是拟合。这是 §6 的内容。

论文 §6.2 说：给定 L(N, Smin)，代入 Smin = Cmin/(6NB)，对 N 求导找最小值，得到 αminC = 1/(1/αS + 1/αB + 1/αN) ≈ 0.054，与 Figure 13 的经验拟合 αminC ≈ 0.050 高度吻合。

【可查证：§6.2 方程 (6.3)–(6.5) 给出了从 L(N, Smin) 推导 L(Cmin)、N(Cmin) 的过程，且明确说 "in excellent agreement with the exponent of Figure 13"】

---

## 对照论文最终方法：迭代链与最终形态的距离

论文最终同时使用了三个层级的拟合：
1. **简单幂律**（v0）：L(N)、L(D)、L(Cmin)——用于 §3 的基础观测和 §1.2 的总结
2. **联合拟合**（v1）：L(N, D)——用于 §4 的过拟合分析和数据需求预测
3. **训练动力学拟合**（v2）：L(N, Smin) + Bcrit(L) 修正——用于 §5 的学习曲线拟合和 §6 的最优分配推导

三层不是替代关系，而是互补：v0 给出最简洁的观测事实；v1 回答"需要多少数据"；v2 回答"需要多少算力和步数"。论文 §6 的最优分配预测之所以有说服力，正是因为它可以从 v2 推导出来并且与 v0 的经验拟合一致。

【可查证：§1.2 总结了三个简单幂律（方程 1.1–1.3）；§4 使用 L(N, D)（方程 1.5）；§5–§6 使用 L(N, Smin)（方程 1.6）和 Bcrit(L)（方程 1.4）；§6.2 明确比较了理论预测与经验拟合的一致性】

---

## 《档案 · S3》

```
【阶段】S3 建模迭代
【已知事实】
1. §4.1 明确列出 L(N, D) 函数形式选择的三条原则（tokenization 缩放、极限退化、D=∞ 处解析）【可查证：§4.1】
2. §4.1 承认第三条原则的论证较弱："Theoretical support for this principle is significantly weaker than for the first two"【可查证：§4.1】
3. §4.2 报告拟合在小 D 端（约 2×10^7 tokens）不好："We obtain an excellent fit, with the exception of the runs where the dataset has been reduced by a factor of 1024"【可查证：§4.2】
4. §5.1 定义 Smin(S) = S/(1 + Bcrit(L)/B) 和 Cmin(C) = C/(1 + B/Bcrit(L))，来源是 [MKAT18]【可查证：§5.1, 方程 5.4–5.5】
5. §5.2 拟合 L(N, Smin) = (Nc/N)^αN + (Sc/Smin)^αS，且说 "The power-law dependence of the loss on Smin reflects the interplay of optimizer dynamics and the loss landscape"【可查证：§5.2】
6. §6.2 从 L(N, Smin) 推导出 αminC ≈ 0.054，与 Figure 13 的经验拟合 ≈ 0.050 "in excellent agreement"【可查证：§6.2】
7. §3.3 承认 L(C) 的拟合在固定 batch size 下不最优："these empirical results are not truly optimal. We will account for this in later sections using an adjusted Cmin"【可查证：§3.3】
8. Figure 23（Appendix D.7）比较了幂律和对数拟合，幂律显著更好【可查证：D.7】
9. 拟合排除规则：1 层网络排除在 L(C) 拟合外；未完全收敛的最大模型排除在 L(N) 拟合外【可查证：D.7】
【合理重构】
a. v0（独立幂律）的"死因"是无法处理联合变量和最优分配问题——依据：论文 §3→§4→§5→§6 的递进结构暗示了这一点（推断）
b. v1（L(N,D)）的修补思路来自三条原则——依据：§4.1 明确列出了这三条原则（可查证，但选择这三条原则的具体动机是推断）
c. v2（L(N,Smin)）的"最后一块拼图"是把训练步数显式纳入并用 Bcrit 修正——依据：§5 的结构和 Sam 的 [MKAT18] 背景（推断）
d. 三项相加的形式 L(N,Smin) = (Nc/N)^αN + (Sc/Smin)^αS 的灵感来自"容量限制 + 优化限制"的物理直觉——依据：论文 §5.2 说 "reflects the interplay of optimizer dynamics and the loss landscape"（推断为物理直觉，非论文明确动机）
【负结果】
1. v0 独立幂律——死因：无法回答 L(N,D) 联合问题、无法做最优分配、L(C) 依赖 batch size 选择（可查证：§3.3, §4, §6 的结构）
2. v1 L(N,D)——死因：不包含训练步数 S、原则 3 论证弱、小 D 端拟合差（可查证：§4.1, §4.2）
3. 对数拟合替代幂律——死因：D.7 的 Figure 23 显示幂律在定性上显著优于对数（可查证：D.7）
4. 对称形式 L(N,D) = [(Nc/N)^αN + (Dc/D)^αD]^β——死因：不具有 1/D 的整数幂级数展开，需要额外参数（可查证：§4.1 脚注 4）
（本阶段负结果 4 条，实质性断言 13 条，占比 31%——累计前四阶段 13/47 ≈ 28%，S5 仍需补足）
【标注覆盖率】本阶段实质性断言共 13 条，其中标【可查证】9 条、【合理重构】4 条（9+4=13）
【措辞自检】本阶段标【合理重构】的段落使用了"推断""依据"等限定词，无断言级副词，无需降级。
【缺席断言检索】本阶段无"论文从未/均未/没有/未提及 X"类断言，无需检索。
【未决问题】
1. Bcrit(L) 的幂律拟合在损失值远离已探索范围时是否可靠？（论文 Caveats 已提及，留给 S5/S8）
2. L(N,Smin) 的拟合残差是否系统性地随模型大小变化？（留给 S8 第 0 步）
3. 最优分配的预测在多大算力范围内可以信任？（留给 S8）
【一致性自检】本阶段推出的判断与 S1/S1.5/S2 档案无冲突——S1 确认了"规模是主变量"的假设（v0–v2 都建立在此上）；S1.5 确认了 Sam 推动最优分配的动机（v2 支撑了 §6）；S2 确认了与 [HNA+17] 的差异（v1 和 v2 的联合拟合是我们独有的）。
【后见之明自检】本阶段未写出具体拟合参数值（αN=0.076 等），未提及 GPT-3/Chinchilla 对 scaling laws 的后续修正——合规。
```

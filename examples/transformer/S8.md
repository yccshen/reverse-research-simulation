# S8 · 对照复盘（幻觉总检查）

> 角色声明：我不再是作者。我是第三方核查员。手里的材料是 S0–S7 的全部档案 + 论文全文（transformer.pdf，v7）。我的任务是找出叙事与论文之间的每一条裂缝——不是为了定罪，是为了量化这套仿真系统的误差。

---

## 第 -1 步 · 图轨（变体 A 降级版，预先声明：不期待独有产出）

**执行记录**：
- 工具：`fig_extract.py` 抽内嵌栅格 + 整页渲染 170 dpi。
- 抽得内嵌栅格 3 张：Fig.1 架构图（p03，1520×2239）、Fig.2 左 scaled dot-product（p04，445×884）、Fig.2 右 multi-head（p04，835×1282）。Fig.3–5 为附录整页渲染（p13–15），无独立内嵌图（热图以 PDF 矢量/整页形式存在）。

**Fig.1（架构图）与 §3 正文逐项对照**：
| 图面元素 | 正文 §3 对应 | 一致性 |
|---|---|---|
| 编码器：Input Embedding → (+Positional Encoding) → [Multi-Head Attention → Add & Norm → Feed Forward → Add & Norm] ×N | §3.1 | 一致 |
| 解码器：Output Embedding → (+Positional Encoding) → [Masked Multi-Head Attention → Add & Norm → Multi-Head Attention（cross）→ Add & Norm → Feed Forward → Add & Norm] ×N → Linear → Softmax | §3.1/§3.2.3 | 一致 |
| "Outputs (shifted right)" | §3.1 "the output embeddings are offset by one position" | 一致 |
| 两处 N× 标注 | §3.1 "N=6" | 一致（图面未标具体数值，正文给定） |
| 残差连接从 sub-layer 输入绕到 Add & Norm | §3.1 "residual connection around each of the two sub-layers, followed by layer normalization" | 一致 |

**Fig.2 左（scaled dot-product）与 §3.2.1 对照**：流程为 MatMul(Q,K) → Scale → Mask(optional) → SoftMax → MatMul(·,V)。与公式 (1) softmax(QK^T/√d_k)V 的运算顺序完全一致，且图面明确标了 Mask 是 optional（对应 §3.2.3 只在 decoder self-attention 里用）。一致。

**Fig.2 右（multi-head）与 §3.2.2 对照**：V/K/Q 各经 h 个并行 Linear → h 个 Scaled Dot-Product Attention → Concat → Linear。与 multi-head 公式 Concat(head₁,…,head_h)W^O 一致。图面 h 的具体数值（8）未标，由正文给定。

**Fig.3–5（附录热图）图注-图面对照**：
- Fig.3 图注说 "attentions here shown only for the word 'making'"，图面确实只从 'making' 一词出发画注意力线，落在 'more difficult' 上——图注与图面一致。
- Fig.4 图注说 "Top: Full attentions for head 5. Bottom: Isolated attentions from just the word 'its' for attention heads 5 and 6"，图面确实上方是全注意力矩阵（head 5）、下方是从 'its' 出发的孤立注意力线（heads 5 与 6 分色）——一致。
- Fig.5 图注说 "two such examples above, from two different heads"，图面上方绿色一组、下方红色一组，确为两个不同的头——一致。
- **无可直读数值**：Fig.3–5 图面无轴、无刻度、无图例（除颜色区分头），任何"某注意力权重 = X"的读数都不可得。此三图在本流程中**不产生可直读量**，符合 S0 的预先声明。

**降级版图轨结论**：架构图与正文逐项一致，无撤销项。本轮图轨的独有产出 = 0，与 S0 第 3 节的预先声明相符。**零产出不是失败，是执行了"价值在撤销"的纪律后确认无可撤销对象**。

---

## 第 0 步 · 重算论文自己的趋势表（在读叙事之前做）

**重算对象 1**：§6.1 "Even our base model surpasses all previously published models and ensembles"（Table 2）
- EN-DE：base 27.3 > 集成最高 ConvS2S Ensemble 26.36 ✓（成立，高出 0.94）
- EN-FR：base 38.1 **<** GNMT+RL Ensemble 41.16、ConvS2S Ensemble 41.29、Deep-Att+PosUnk Ensemble 40.4 ✗（不成立，低 2.28–3.19）
- **判定：该概括句在 EN-DE 上成立、在 EN-FR 上不成立。** 论文未限定语言对，属**概括句的过度泛化**。严重度：论文自身问题（不是本仿真的幻觉），记入"论文自身的问题"清单，供 S9 附录《勘误与对照》使用。

**重算对象 2**：§6.2 "quality also drops off with too many heads"（Table 3 行 A）
- h=1: 24.9；h=4(dk=128): 25.5；h=8(base): 25.8；h=16: 25.8；h=32: 25.4；h=32(dk=16): 25.4；h=16(dk=16): 25.8（最后一个为 dk/dv=16、h=16 的配置）
- 从 h=8 到 h=16 BLEU 持平（25.8→25.8），到 h=32 才降（25.4）。"drops off with too many heads" 在 h=16 上不成立、在 h=32 上成立——**概括句方向对、阈值模糊**。论文自身问题（轻微）。

**重算对象 3**：§6.2 "bigger models are better"（Table 3 行 B + 行 C）
- d_model=256: 26.0；d_model=1024: 26.0；base(d_model=512): 25.8。256→512→1024 的 BLEU 为 26.0→25.8→26.0——**非单调**。PPL 方向单调（4.66→4.92→4.33，1024 最优）。
- N=2: 23.7；N=6(base): 25.8——层数方向单调。
- **判定："bigger is better" 在层数上单调、在 d_model 上不单调（BLEU 口径）。** 论文自身问题（轻微，PPL 口径可救）。

**重算对象 4**：§3.5 "learned positional embeddings 与 sinusoidal 结果几乎一样"（Table 3 行 E）
- 行 E: PPL 4.92, BLEU 25.7；base: PPL 4.92, BLEU 25.8。差 0.1 BLEU、PPL 相同——"几乎一样"成立 ✓。

**重算对象 5**（S0 前置疑点）：§6.1 的 "41.0" vs Abstract/Table 2 的 "41.8"
- 三处出现：Abstract 41.8 ✓、Table 2 Transformer(big) 行 41.8 ✓、§6.1 正文 41.0 ✗
- **判定：论文自身的数字不一致，两处 41.8、一处 41.0。** 按 S0 的处理路径，本篇无图能直接算出 41.8 或 41.0（无训练曲线图），只能定性为**正文口径残留**。S7 审稿人独立发现同一条（交叉验证）。

**重算对象 6**：Table 1 复杂度表与 §4 正文对应
- Self-Attention 行：O(n²·d) / O(1) sequential / O(1) path length——§4 正文对应成立 ✓
- Recurrent 行：O(n·d²) / O(n) / O(n)——§4 对应成立 ✓
- Convolutional 行：O(k·n·d²) / O(1) / O(log_k n)——§4 "a stack of O(n/k) ... or O(log_k n) in the case of dilated convolutions" ✓
- Restricted self-attention 行：O(r·n·d) / O(1) / O(n/r)——§4 "would increase the maximum path length to O(n/r)" ✓
- 四格全对，Table 1 内部一致。

**重算对象 7**：S7 审稿人 MW2 的算术复核
- "base 模型 EN-DE 3.3e18 vs ConvS2S 9.6e18 仅约 3 倍"：3.3e18 / 9.6e18 = 0.344，即 base 是 ConvS2S 的 34%，差约 2.9 倍——审稿人算术正确 ✓
- "big vs GNMT+RL Ensemble 约 48 倍"：2.3e19 / 1.1e21 ≈ 0.021，即 1/48——S7 rebuttal 里的算术正确 ✓
- "big vs GNMT+RL 约 6 倍"：2.3e19 / 1.4e20 ≈ 0.164，即 1/6.1——S7 rebuttal 里的算术正确 ✓

---

## 1. 【对上了】（叙事与论文一致，共 17 条）

| # | 叙事条目 | 论文出处 | 一致性 |
|---|---|---|---|
| 1 | S1：顺序性阻碍并行 + 内存限制 batching | §1 原句 | ✓ |
| 2 | S1：factorization [21] / conditional computation [32] 已试但不根本 | §1 原句 | ✓ |
| 3 | S1：Jakob 提出用 self-attention 取代 RNN | 第 1 页脚注 | ✓ |
| 4 | S1.5：8 人等贡献、署名顺序随机 | 第 1 页脚注 | ✓ |
| 5 | S2：ConvS2S/ByteNet 路径长度随距离增长 | §2 原句 | ✓ |
| 6 | S2：§2 自述"据我们所知是第一个纯 self-attention 转导模型" | §2 原句 | ✓ |
| 7 | S3：scaled dot-product 的方差推导（脚注 4） | §3.2.1 | ✓ |
| 8 | S3：multi-head 动机句（averaging inhibits） | §3.2.2 原句 | ✓ |
| 9 | S3：正弦式位置编码公式 + 选它的理由（外推性） | §3.5 | ✓ |
| 10 | S3：学习式与正弦式结果几乎一样 | Table 3 行 E | ✓ |
| 11 | S4：batch 按句长分桶、25K+25K token | §5.1 原句 | ✓ |
| 12 | S4：Adam β2=0.98、warmup=4000 | §5.3 | ✓ |
| 13 | S4：base 12 小时、big 3.5 天、8×P100 | §5.2 | ✓ |
| 14 | S5：RNN 在小数据句法分析上未达到 SOTA | §6.3 原句 | ✓ |
| 15 | S6：Fig.3–5 放附录、正文未引用图号 | 全文检索 | ✓ |
| 16 | S6：§7 三条 future work 与毙掉的方向一一对应 | §7 原文 | ✓ |
| 17 | S7：审稿人抓到的 41.0/41.8、FLOPs、路径长度未验证 | §6.1/§6.1脚注5/§4 | ✓（审稿人独立发现，说明这些问题在论文文本层面真实存在） |

## 2. 【编圆了但论文没这回事】（幻觉清单）

**P0（与论文事实直接矛盾）：0 条。**
第四轮缺席断言禁令 + 两个必填检查点字段的组合起效：前三轮每轮一条的 P0 形态（"全文未提 X"而实际多处出现）在本轮**零复发**。S1–S7 的每一档案块都附了检索记录，S8 复核时逐条抽验，未发现任何一条"论文从未／均未／没有"式断言与论文事实矛盾。

**P1（因果夸大或论据错位）：2 条。**

| # | 出处 | 幻觉内容 | 论文事实 | 严重度 |
|---|---|---|---|---|
| P1-1 | S3 v0 重建 | "d_k 从 8 抬到 32、64 时模型不收敛，loss 在前 5K 步几乎平着"——叙事把"不收敛"作为亲历观察写出 | 论文 §3.2.1 只给理论推导（脚注 4）与 "We suspect that..."，没有任何"我们观察到不收敛"的实验记录；§5 无任何训练不稳定的讨论 | P1：把理论动机写成了实验观察，因果夸大 |
| P1-2 | S5 至暗时刻一 | "空句引发 masked softmax 的 0/0，是 NaN 的根因" | 论文全文无 NaN / 崩溃 / 空句的任何记录（S5 已自标"根因未完全确认"，但叙事正文用了可复现的最短解释链这种近似肯定的语气） | P1：根因是重建，叙事语气过实 |

**P2（标签错配与表述不严）：2 条。**

| # | 出处 | 内容 | 问题 |
|---|---|---|---|
| P2-1 | S5 至暗时刻四 | "连夜跑了一个不分桶的对比，BLEU 差 0.2" | 该实验的存在本身是最弱重建（论文无记录），叙事正文以陈述句写出而非"我重建了这样一次实验" | P2：重建与事实的边界模糊 |
| P2-2 | S4 第 9 节 | 硬数字清单含 "2.3e19, 3.3e18" 等 FLOPs 值 | 这些是估算值（论文自己声明是 estimate），叙事中作为"硬数字"列出而未标"估算" | P2：术语不严（影响小） |

**幻觉总计：P0 = 0，P1 = 2，P2 = 2，共 4 条。**
**断言总数（S1–S7 档案块中【可查证】+【合理重构】之和）：S1: 18 + S1.5: 15 + S2: 14 + S3: 19 + S4: 22 + S5: 16 + S6: 14 + S7: 12 = 130 条。**
**P0–P2 总量占比：4 / 130 ≈ 3.1%。**
**对比前三轮**：GPT-1 首跑 17%（P0 2 条）→ GPT-2 第二轮 18.2%（P0 1 条）→ MI-1 第三轮 12.1%（P0 1 条）→ **本轮 3.1%（P0 0 条）**。降幅显著，但需注意：本篇论文短（15 页）、结构规整（表格承载全部数字），核验难度客观上低于 GPT-1/2 与 MI-1——**不能把这 3.1% 全归功于新条款**。

## 3. 【论文有而叙事漏了】（覆盖度缺口）

1. **Table 2 完整 FLOPs 量级对比**：S4/S7 引用了部分（GNMT+RL 1.4e20、GNMT+RL Ensemble 1.1e21、ConvS2S 9.6e18、ConvS2S Ensemble 1.2e21、MoE 2.0e19、ByteNet 无 FLOPs、Deep-Att+PosUnk 1.0e20、Deep-Att Ensemble 8.0e20），但 Deep-Att + PosUnk Ensemble 的 8.0e20 未在任何阶段被引用——它是 Table 2 里 EN-FR 方向的第二贵模型。
2. **§6.1 的推理超参数**：beam size=4、length penalty α=0.6、max output = input+50、early termination——S4 第 6 节的超参表收了 beam/α，但 max output 与 early termination 未收。
3. **Table 4 的 beam size=21 与 α=0.3**（句法分析的推理超参与翻译不同）——S4/S5 均未提。
4. **§3.4 embedding 层权重乘 √d_model**——S3 四象限表列了 embedding 共享，但乘 √d_model 这一条未在四象限表或正文任何地方被单独登记。

## 4. 论文自身的问题（本流程的独立产出，与叙事幻觉无关）

| # | 问题 | 位置 | 严重度 |
|---|---|---|---|
| R-1 | EN-FR BLEU 三处两值：Abstract 41.8 / Table 2 41.8 / §6.1 正文 41.0 | §6.1 vs Abstract/Table 2 | **高**（S7 审稿人独立发现，交叉验证） |
| R-2 | "base model surpasses all previously published models and ensembles" 在 EN-FR 上不成立（38.1 < 41.29） | §6.1 vs Table 2 | 高（概括句过度泛化） |
| R-3 | "quality also drops off with too many heads" 在 h=16 上不成立（25.8 = base），h=32 才降 | §6.2 vs Table 3 行 A | 中 |
| R-4 | "bigger models are better" 在 d_model 上非单调（256: 26.0 → 512: 25.8 → 1024: 26.0，BLEU 口径） | §6.2 vs Table 3 行 B | 中 |
| R-5 | 路径长度（§4 核心动机）未在任何实验中被孤立验证——Table 1 是理论对比，Fig.3 是定性可视化，均不构成 controlled evidence | §4 全文 | 中（S7 MW3b 独立发现） |
| R-6 | [37] 与 Table 4 中 "Vinyals & Kaiser el al." 应为 "et al." | 参考文献 [37] 与 Table 4 | 低（S7 MI1 独立发现） |
| R-7 | §3.5 encoding/embedding 两词混用（sinusoidal 也称 encoding、learned 也称 embedding，但论文自己把 learned 当另一种机制） | §3.5 与 Table 3 行 E | 低（S7 MI2 独立发现） |

**隔离审稿人失准核查（MI-1 轮固化的反向义务）**：
- MW1（41.0/41.8）：**准确**。与 S0 前置疑点 + S8 重算对象 5 完全吻合。
- MW2（FLOPs 跨硬件）：**算术全对**（3.3e18/9.6e18≈0.344 ✓），方法学指控合理。但有一处轻微过火：说 "a small fraction" 宣称对 base 模型 "远谈不上"——base/big 的区分在 §6.1 里其实有（"Even our base model surpasses..." 后面跟着 "a fraction of the training cost of any of the competitive models"），审稿人没有引用这句 "any of the competitive models"（它指的是 single models，不是 ensembles——这又回到 R-2 的概括句问题）。轻微失准，不构成撤销。
- MW3a（消融噪声）：**准确**，Table 3 多行 BLEU 差异确实 ≤0.5，无显著性检验。
- MW3b（路径长度未验证）：**准确**，§4 全文无 controlled experiment。
- MI1/MI2：**均准确**。
- **审稿人总失准率：0 条完全错误，1 条轻微过火（MW2 的 "远谈不上"）**。本轮隔离审稿人的可靠性显著高于 MI-1 轮（那次撤了 2 条审稿人指控）。

## 5. 论文正面硬数字吸收核查

§6.1 的 28.4/41.8、Table 3 的 25.8/4.92、Table 4 的 91.3/92.7、§5.2 的 12h/3.5d/0.4s/1.0s——全部被 S4/S5 的叙事吸收（S4 第 9 节有完整清单）。**无遗漏**。

## 6. 这套仿真教会我什么（分层声明）

**读论文原文绝对学不到的**（流程产出）：
- 论文自己没写的：β2=0.98 与 warmup=4000 背后的故事（S5 至暗时刻一）；§7 三条 future work 与毙掉方向的对应关系（S6 第 6 节）；Fig.3–5 放附录的防守逻辑（S6 第 2 节）。
- 论文写了但读者通常忽略的：Table 2 的 FLOPs 列是叙事第二根柱子（S1.5 分歧二）；§6.3 立项理由是"RNN 在此有案底"（S1.5 分歧三）。

**本流程特有的方法学产出**：
- 第四轮条款（缺席断言禁令 + 检查点字段）起效：P0 零复发。这是三轮以来第一次。
- 隔离审稿人的可靠性在不同轮次间方差极大（MI-1 轮撤 2 条 vs 本轮撤 0 条）——**"隔离"只保证视角独立，不保证结论可靠**这条 MI-1 轮的经验在本轮得到反向确认。
- 论文自身的 R-2（"base surpasses all ensembles" 在 EN-FR 上不成立）是**两轮视角都漏了**的发现——S7 审稿人没抓（他抓了 R-1 的 41.0/41.8 但没抓 R-2 的过度泛化），叙事各阶段也没抓。它是 S8 第 0 步"先重算趋势表"的产物——**这一步仍是全流程性价比最高的一步**。

---

```
《档案 · S8》
【阶段】S8 对照复盘
【已知事实】
1. 幻觉清单：P0=0，P1=2，P2=2，共 4 条，占 130 条断言的 3.1%；
2. 论文自身问题 7 条（R-1 至 R-7），其中 R-1/R-2 为高严重度；
3. 隔离审稿人 3 条 major + 2 条 minor 的独立发现与 S8 复核交叉验证一致（除 MW2 一处轻微过火）；
4. 图轨降级版执行完成：Fig.1/2 与 §3 逐项一致，Fig.3–5 无可直读数值，零撤销项；
5. 覆盖度缺口 4 条（Deep-Att Ensemble FLOPs、推理超参细节、句法分析 beam/α、√d_model）；
6. P0–P2 占比历史：17% → 18.2% → 12.1% → 3.1%。
【合理重构】无（本阶段为核查报告，不做新断言）。
【负结果】本阶段的"负结果"是论文自身的问题清单（R-1 至 R-7），不是仿真的失败。仿真自身的失败是 4 条幻觉（P1-1/P1-2/P2-1/P2-2）。
【标注覆盖率】不适用（本阶段为核查报告，不做来源标注）。
【措辞自检】不适用。
【缺席断言检索】本阶段出现 1 处缺席类断言，附检索记录：
  "§5 无任何训练不稳定的讨论"→ 检索词 "NaN"、"diverge"、"instability"：全文 0 命中（已在 S5 档案登记，此处复核一致）→ 断言保留。
【未决问题】
1. 4 条覆盖度缺口是否补进 S9 成稿——按 S9 素材边界条款，默认不补，留在覆盖度清单；
2. R-1 至 R-7 是否写入 S9 附录《勘误与对照》——按 S9 规则，S8 的三张表压缩进附录，R 系列作为"论文自身问题"单列一节。
【一致性自检】与 S0–S7 全部档案一致，无新冲突。
【后见之明自检】不适用（本阶段为核查视角，本来就站在终点回望）。
```

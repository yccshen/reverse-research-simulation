# S1 · idea 诞生

> 纪律声明（写在这里，也写在心里）：我不知道这个项目最后会成功；我不知道最终的方法长什么样；下面每个判断都打了标签，标了【合理重构】的地方，依据写在括号里。

## 一、我当时在做什么

我在 Google Brain 做机器翻译，日常是两类活：调一个已经很能打的 LSTM 编码器-解码器（就是 [38] 那套 GNMT 的亲戚），和回答"为什么这版比上版慢了三小时"。

真正刺到我的不是某个数字，是一句我自己说过很多遍的话：**每一步的隐状态 h_t 必须等 h_{t-1} 算完**（【可查证】§1 把这个说成 "This inherently sequential nature precludes parallelization within training examples"）。这句话当时在我们内部是常识，是"物理定律"级别的常识。而我每天的工作有一大半是在跟这个定律讨价还价：把 batch 拼得再满一点、把句子按长度分桶、把句子长度截在 50 词。讨价还价的结果是：**任何一次架构级的想法，都要排在一周之后才能知道好不好**。一次实验 12 小时以上，一天只能试一次。这个节奏下，你不敢想大改动，只能想小补丁。

所以我最早的动机其实很土：**我想让实验快起来**。不是"我想提出新架构"。

## 二、我的第一个 naive 想法

最直接的想法是：**不动架构族，把 LSTM 本身变瘦**。

当时手边有两条现成的路：【合理重构】（依据是论文 §1 自己点名的两篇：）[21] 的 factorization tricks，把大矩阵拆成小块乘；[32] 的 MoE / conditional computation，让每一步只激活一部分参数。两条路都有人在语言模型上做出过收益，套到翻译上也顺理成章。我的第一版方案就是这么写的：**保留 LSTM 的骨架，把每步的计算量压下去，让训练时钟时间减半**。甚至很具体——先动 encoder 的 LSTM 层，把 8 层里的大矩阵按维度拆开，再把 FFN 换成稀疏门控。

它死在哪儿，现在说出来很直白，但当时花了我不短时间才承认：**这两条路优化的都是"每一步内部算得多快"，而卡住我的是"你必须算 n 步"**。也就是说，我动的是常数因子，墙是结构性的。把每一步的成本砍一半，100 步还是 100 步，长句上的显存墙一点没动（【可查证】§1："memory constraints limit batching across examples"——句子一长，你就没法靠加 batch 摊薄，因为激活要沿整个时间步留着）。我用两周做出了"每步更快"的版本，跑出来的曲线几乎重叠，然后我意识到我把力气花在了错的那一列上。

【负结果 1】把 batch 做大来摊薄单步开销 → 被显存挡住：长句的激活要贯穿时间步保留，batch 一大就爆。这条没得救，是硬件级的。
【负结果 2】只并行 encoder，decoder 保留 LSTM（encoder 换成卷积或自注意力的并行块）→ 技术上能跑，也能把**训练**成本压下来。它死在两个地方：一是我说不清"训练快了但推理没快"算不算成果（训练时的 teacher forcing 让 decoder 本来就能并行，推理时还是逐词吐）；二是当时的评测口径只认 BLEU，训练快不体现在分数上。这个方向没有被我埋掉，它活成了论文最后一节的一句话："Making generation less sequential is another research goal of ours"（【可查证】§7）。
【负结果 3】局部/受限注意力先上（只在窗口内做注意力，把 O(n²) 压成 O(n·r)）→ 这一版我实际算过账：句子在翻译任务里通常短于表示维度，窗口一限，长距离依赖就得靠层数堆回来，路径长度从 O(1) 退化成 O(n/r)。为了治一个当时并不疼的病（超长序列），牺牲一个当时很疼的能力（长距离对齐）。搁置——但我在 §4 里留了它的位置（【可查证】§4 末段 "self-attention could be restricted to considering only a neighborhood of size r"，列为 future work）。

## 三、转机

Jakob 在一次讨论里问了我一句我答不上来的话：**"我们为什么还需要循环？"**（【可查证】首页脚注：Jakob 提出用 self-attention 取代 RNN 并启动了这项评估。那句问话的措辞是我的重构，问话这件事本身是论文自己写的。）

我当时的本能反应是"因为顺序信息需要它"。然后我发现这个回答撑不住三个反例：
1. 注意力机制本身早就能无视距离地建模依赖了（【可查证】§1 引 [2, 19]）；
2. Parikh 等人的 decomposable attention 已经在若干任务上证明了"只有注意力也能工作"，而且 Jakob 就在作者列表里（【可查证】文献 [27]）；
3. 卷积派的 ByteNet 与 ConvS2S 已经把"并行化本身能换来什么"摆出来了，它们只是还没敢把循环彻底扔了（【可查证】§2）。

于是问题被我重新提了一遍，这一遍才是可算的：**循环到底提供了什么不可替代的东西？** 我的答案是两样：位置信息，和长短依赖的可学习性。前者看起来可以用别的方式注入（怎么注入是后面的事）；后者有个现成的判据——**信号在图上要走多远**：循环是 O(n) 步，卷积分层是 O(log_k n)，如果全连接式注意力成立，那就是 O(1)（【可查证】§2 与 §4："reduced to a constant number of operations"）。路径长度这个尺子一拿出来，"去掉循环"从一句激进的口号变成了一道可以计算、可以比较的题。

这也是我这一周唯一真正的收获：**换的不是模块，是提问方式**。

---

```
《档案 · S1》
【阶段】S1 idea 诞生
【已知事实】
1. 循环模型的顺序性阻碍样本内并行，长序列时尤其致命（内存限制 batching）——论文 §1 原句；
2. 已有工作用 factorization [21] 与 conditional computation [32] 提升效率，但"顺序计算的根本约束仍然存在"——§1 原句；
3. 注意力机制已能无视距离建模依赖（[2,19]），但"除极少数例外 [27]，注意力都是与循环网络配合使用"——§1 原句；
4. 卷积派（Extended Neural GPU [16]、ByteNet [18]、ConvS2S [9]）已在并行计算隐表示上先行；其相对位置的操作数随距离增长（ConvS2S 线性、ByteNet 对数）——§2 原句；
5. self-attention（intra-attention）此前用于阅读理解、摘要、蕴含、句表示 [4,27,28,22]——§2 原句；
6. 论文自述"据我们所知，Transformer 是第一个完全依赖 self-attention 计算输入输出表示、不使用序列对齐 RNN 或卷积的转导模型"——§2 原句；
7. 作者脚注自述：Jakob 提出"用 self-attention 取代 RNN"并启动了这项评估——第 1 页脚注；
8. 受限（窗口）自注意力被列为 future work，代价是最大路径长度变为 O(n/r)——§4 末段；
9. "让生成过程不那么串行"被列为另一个研究目标——§7 末段。
【合理重构】
1. 一作当时的痛点是实验时钟时间与长句显存墙，而非"要提出新架构"（依据：§1 把顺序性直接翻译成并行性与内存问题，且全文把训练成本当作一等指标，见表 2 的 FLOPs 列）；
2. 第一个 naive 方案是"保留 LSTM 骨架、压单步成本"（依据：§1 专门点名 [21][32] 两条效率路线并立刻否定其根本性，这类句子通常来自亲历的失败）；
3. 存在过"只并行 encoder、保留 LSTM decoder"的中间方案（依据：§7 把"让生成不串行"单列为待办，说明解码端的串行性是当时明确放弃而非忽略的一支）；
4. 用"每步计算量 vs 步数"的区分来复盘 v0 的死因（依据：§4 的三条 desiderata 里第二条正是"最少的顺序操作数"，与 Table 1 第三列同源）；
5. Jakob 的问话措辞与场合（依据：脚注只写了"提出并启动"，未给措辞）。
【负结果】
- v0（保留 LSTM、压单步成本）：死因为优化常数因子而非结构上限，表现为"每步更快但曲线重叠"；
- v0 配套的"加大 batch 摊薄"：死因为长序列激活贯穿时间步，受显存硬限制；
- 只并行 encoder：死因为训练可并行而推理仍串行，评测口径不认训练成本（该支线转为 §7 的 future work）；
- 受限注意力先上：死因为当时序列长度短板不疼，而路径长度退化疼（转为 §4 的 future work）。
负结果篇幅占比约 45%（三段负结果 + v0 复盘，占正文 1200 字的约 540 字）。
【标注覆盖率】本阶段实质性断言共 18 条：【可查证】9 条（正文中逐条给出处），【合理重构】9 条（含 5 条重建条目 + 4 条负结果），9+9=18。
【措辞自检】初稿有 1 处把 v0 的死因写成"这就是根本原因"，已降级为"这可能是主因"；现存【合理重构】段落中无"就是／正是／唯一／显然／必然／从来"类断言级副词（"唯一真正的收获"一处经复核属于自述个人体验，非对论文的断言，保留）。
【缺席断言检索】本阶段出现两处缺席类断言，均附检索记录：
  （a）"当时没有人把 self-attention 用作翻译骨干"→ 检索词："self-attention"、"intra-attention"（论文全文，忽略大小写），命中 24 处；命中段落中 §2 列举的 self-attention 应用为阅读理解／摘要／蕴含／句表示 [4,27,28,22]，未见用于机器翻译者，且 §2 自述"据我们所知是第一个"。断言保留，但其作用域仅限于"论文的叙述里没有"，不等于对 2016 年前全球文献的普查（我未做文献普查）。
  （b）"论文当时没有给路径长度以外的判据"→ 检索词："path length"、"sequential operations"，命中见 §4 与 Table 1；仅两条判据（复杂度、顺序操作数）加路径长度共三条，断言保留。
  旁证计数：全文 "recurrent|RNN|LSTM" 25 处，"convolution" 16 处，"attention" 84 处。
【未决问题】
1. 位置信息怎么注入、注入成什么形式（正弦 vs 学习式）——留给 S3；
2. decoder 的串行生成是否必须接受——留给 S3/S5；
3. 用什么实验在最短时间内证明"去掉循环不亏质量"——留给 S4。
【一致性自检】与《档案 · S0》无冲突。S0 标出的"主实验三处"与本阶段提到的判据（路径长度、复杂度）同源，未出现事实对撞。
【后见之明自检】未泄露最终结果：本阶段没有写出 multi-head、scaled dot-product、位置编码公式、消融结论中的任何一项；"O(1) 路径长度"一词确实等于最终架构的性质，但它是 §4 第三条 desiderata 的前身，且我在本阶段只把它当作判断尺子提出、未当作已有方案。此处按可疑处理，请在 S8 复核本句的依据链：依据是 §2 的 "reduced to a constant number of operations"（论文自述，属【可查证】），链条成立。
```

# S0 · 选材闸（2026-09-17）

## 论文基本信息
- **标题**：Attention Is All You Need（Vaswani et al., NIPS 2017）
- **来源**：arXiv:1706.03762，本目录 transformer.pdf 为 **v7（2023-08-02 更新版）**；v1 提交于 2017-06-12。仿真以发表时内容为准，用 v7 时注意它不是当时原件。
- **作者与单位**：8 人，Google Brain（Vaswani/Shazeer/Kaiser/Polosukhin*）、Google Research（Parmar/Uszkoreit/Jones）、University of Toronto（Gomez）。全部标等贡献，脚注注明"排名随机"，且逐一写了分工——【可查证】第 1 页脚注。

## 闸检查（四条全过）
| 检查项 | 结论 | 依据 |
|---|---|---|
| 单方法论文 | ✅ | 提出一个架构（Transformer），非综述/benchmark/系统报告 |
| 核心 claim 明确、主实验可定位 | ✅ | claim：纯注意力（去循环去卷积）→ 质量 SOTA + 训练成本大降 + 高度可并行。主实验 3 张表：Table 2（WMT14 EN-DE 28.4 / EN-FR 41.8 BLEU + FLOPs）、Table 3（架构变体消融，newstest2013 dev）、Table 4（WSJ 成分句法 91.3/92.7 F1） |
| 有代码/足够细节 | ✅ | tensor2tensor 开源（结论节给链接）；§3–§5 超参与训练配方完整 |
| 用户前置知识 | ✅ | 已仿真 GPT-1、GPT-2 两轮，Transformer 是它们的前置；竞赛数学背景可吃 §3.2 的 √dk 缩放推导 |

## 形态判定：**变体 A（纯文本/表格驱动）**
- 核验物是**表格**（Table 2/3/4），不是图；主图 Fig.1/Fig.2 是架构示意图，Fig.3–5 是附录注意力可视化，无器件/湿实验环节。
- 方法迭代落在**架构与超参**，不是配方工序 → 不走变体 B，不启用图轨工具链（S8 时 Fig.3–5 只作辅助对照）。

## S1.5 建议：提前到第一轮
三个单位、8 位作者、脚注分工明确（Jakob 提出用 self-attention 替 RNN 并发起验证；Ashish+Illia 实现首批模型；Noam 提出 scaled dot-product、multi-head、无参数位置表示；Niki 调了无数变体；Llion 管初始代码库与推理；Lukasz+Aidan 造 tensor2tensor）→ 满足"多作者论文把 S1.5 提前"的条件，且脚注就是现成的博弈素材。

## 本轮特殊风险（相对前三轮）
1. **共识记忆极密**：这是被解读得最多的深度学习论文之一，"无后见之明"约束会比 GPT-1/2 轮更弱，S8 后见之明审查要更狠。
2. **前三轮唯一 P0 形态（缺席断言）的复发风险照旧**，且本文文本短、引用密（40 篇参考文献），"全文未提 X"式断言极易翻车。
3. 版本陷阱：我们手里是 v7 而非 2017 年原件，叙事里凡"当时"的表述要防被 v7 措辞带偏。

---

## 交接：后面每一阶段要用的锚点（提前标出，不许到用时现找）

- **S1 的"现状"材料**：Abstract + §1 Introduction + §2 Background（上面的基本信息一并给出）。
- **S3 的对照锚点**：§3 全部参数（N=6、d_model=512、h=8、d_k=d_v=64、d_ff=2048、P_drop=0.1、label smoothing 0.1、warmup 4000）+ §4 的三条 desiderata（每层复杂度／最少顺序操作数／最长路径长度）。
- **S4 的核验锚点**：Table 2（BLEU + 训练 FLOPs，含各 baseline 的 FLOPs 量级）、Table 3（变体消融行 A–E）、Table 4（句法分析）；§5.2 硬件与耗时（8×P100；base 10 万步 12 小时、单步 0.4 秒；big 30 万步 3.5 天、单步 1.0 秒）。
- **S8 第 0 步的重算对象**（本轮前置标定，届时按此逐行算差值）：
  1. §6.1 那句 "Even our base model surpasses all previously published models and ensembles" —— Table 2 里 EN-DE base 27.3 高于集成最高 26.36（成立），但 EN-FR base 38.1 **低于**集成 41.29 与 40.4（不成立）。该句是否构成概括句反例，交 S8 裁定。
  2. §6.2 "quality also drops off with too many heads" 与 "bigger models are better" —— 对 Table 3 行 (A) 与行 (C) 逐行做差值，看单调性是否真成立。
  3. §3.5 末尾 "learned positional embeddings 与 sinusoid 结果几乎一样" 与 Table 3 行 (E) 的 4.92/25.7 vs base 4.92/25.8 是否一致。
  4. Table 1 复杂度表与 §4 正文的对应关系逐格核对（尤其 restricted self-attention 一行与正文 O(n/r) 的说法）。
- **S8 需要声明为【不可核验】的项**：未能与 NIPS 2017 定稿件逐字比对（手上为 v7）；Fig.3–5 的热图无可直读数值（图面只有词序列与颜色，无轴、无刻度）；训练曲线的中间过程（论文只给终值与总耗时）。

## 已知待裁定的数字口径（首轮读稿时就撞见，留给 S8）

§6.1 正文写 EN-FR 大模型 "achieves a BLEU score of 41.0"，而 Abstract 与 Table 2 均为 **41.8**。同一文档内两个数，属"两个都合理但不同"的类型 —— 按模板要求，S8 处理这类问题**先去找有没有一张图能直接算出其中一个**（本篇无此图，预计只能定性为正文口径残留）。

## 修订注（2026-09-17，模板第四轮条款生效后回填）

本文件前面写的"变体 A → 不启用图轨"一句按新条款修正：**图轨已升为 S8 必跑项，变体 A 跑降级版**（对照架构图 Fig.1/Fig.2 与正文 §3 逐项是否一致；对照附录 Fig.3–5 的图注与图面是否一致）。同时预先声明：本篇图轨**不期待独有产出**（示意图无标定物），若能撤销叙事里的假发现，即算完成它的职责。原句不删，留作改动痕迹。

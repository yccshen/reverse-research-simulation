# S1 · idea 诞生 —— GPT-1 科研全仿真

> 口径三段式
> - 目标：还原"生成式预训练 + 判别式微调"idea 诞生前一周的第一作者视角。
> - 现状：论文 abstract + introduction。时间点设为 2018 年 3 月（论文 2018 年 6 月挂出；ELMo/ULMFiT 刚出两个月，GLUE 尚不存在）。
> - 限制：只用 2018 年 3 月前公开材料；禁止引用论文实验结果。

## 1. 我当时在做什么，什么刺到了我

过去一年我的主线是无监督表征。2017 年我和 Rafal、Ilya 做了那个 Amazon 评论的实验：一个 byte 级的 mLSTM，纯粹用下一个字预测来训，训完发现里面长出了一个专门管情感的神经元——掰动它，生成文本的情感极性就跟着翻转【可查证：Radford, Jozefowicz, Sutskever 2017】。这件事给我留下一个执念：**语言模型内部已经悄悄学会了"任务"，只是没人把它系统地挖出来**。之后我去做 block-sparse 的 GPU kernel【可查证：Gray, Radford, Kingma 2017，即论文引文[16]】，说白了就是想让这类大模型训得起。

刺到我的是 NLP 领域的现状：词嵌入早就是标配了，但"比词更高的东西"——句子级、篇章级的知识——没人迁得动。原因就两条：一，用什么目标函数学无监督表征，没有共识（LM、翻译、篇章连贯各赢各的）；二，学到的表征怎么迁到下游，没有共识【可查证：论文 §1 的两点陈述本身就是在描述这个困境】。

## 2. 我的 naive 想法

最直接的：顺着 sentiment neuron 往下做。把无监督预训练好的大 LSTM 的隐状态抽出来，当**特征**喂给每个任务的专用模型。这其实就是 CoVe【可查证：McCann et al. 2017，引文[38]】和上个月刚出的 ELMo【可查证：Peters et al. 2018.2，引文[44]】的路线。我的想法甚至有点偷懒：他们用 BiLSTM，我用我熟的大 LSTM，特征质量不会差。

## 3. 它卡在哪，为什么死

- **特征路线有个结构性死穴**：迁过去的只是"表示"，任务模型本身是随机初始化的、任务专用的。ELMo 在下游还得叠任务自己的 BiLSTM + attention，每个任务新增一大堆参数【可查证：论文 §2 对 [43][44] 的批评——"a substantial amount of new parameters for each separate target task"】。也就是说无监督学到的东西只负责输入层，真正的推理全靠标注数据现场学——标注少的任务照样崩。这和我"模型内部已经会了"的信念直接矛盾。
- **LSTM 本身也到头了**：sentiment neuron 那个 4096 维 byte 级模型训了一个月量级【可查证：Radford 2017 论文训练设置】，慢得离谱；而且 LSTM 对长程依赖是"一条状态向量硬扛"，几百词之前的信息基本磨没了。做句子级任务勉强，做篇章级推理没戏。
- **更难受的是，坑位有人占了**：Dai & Le 2015 年就做过"LSTM 语言模型预训练 + 微调"【可查证：引文[13]】，Howard & Ruder 今年 1 月刚把它做到位【可查证：ULMFiT，引文[21]】——但都只打在文本分类上。NLI、QA 这种"输入是两三段文本"的结构化任务，这条路线没人走通。

## 4. 转机

两个东西撞在了一起。

一是 **Transformer**。Vaswani 他们去年底的翻译模型【可查证：[62]】，加上 Peter Liu 他们年初拿 Transformer decoder 做长文生成【可查证：[34]，引文时间 2018 年初】——证明了这个架构当纯语言模型用是成立的。关键是它的记忆方式：attention 让每个位置直接看回所有历史位置，是结构化的记忆，不是一条向量硬扛。

二是 **Rocktäschel 2015 年那个 traversal 式做法**【可查证：[52]】：把前提、假设这种结构化输入用分隔符拍平成一条序列，让模型自己去处理。如果这招成立，"结构化任务没法微调"的死结就解了——**不用改架构，改输入就行**。

拼图在脑子里合上了：Transformer decoder 当 LM 预训练 → 所有任务统一拍平成序列 → 只在最后加一个线性层微调。架构零改动，预训练的每一层参数都参与下游任务。这个想法成不成立，我一点把握都没有——但至少它和我 2017 年那个执念是自洽的。

---

## 《档案 · S1》

```
【阶段】S1 idea 诞生
【已知事实】
1. 作者 2017 年做过 byte 级 mLSTM 情感神经元工作【可查证·论文外：Radford, Jozefowicz, Sutskever 2017。注意标签边界：该文不在论文参考文献表中，无法在"论文全文"这一闭环内核验；论文 Table 4 以引文[16]（block-sparse kernels）指代同源的 Sparse byte mLSTM 行——修订注：原稿把此条笼统标为【可查证】，现补注其来源在论文之外（S8 第 2 节 H7）】
2. 作者同期做 block-sparse GPU kernel（论文引文[16]，Gray/Radford/Kingma 2017）
3. 迁移困境两点（目标无共识、迁移方式无共识）——论文 §1 原文
4. CoVe=MT 特征[38]、ELMo=BiLSTM 特征[44]（2018.2）、均留大量任务专用新参数——论文 §2
5. Dai & Le 2015[13] 与 ULMFiT[21]（2018.1）做了 LSTM 预训练+微调，仅覆盖文本分类——论文 §2
6. Transformer[62]（2017）与 Transformer decoder 长文生成[34]（2018 初）已公开
7. traversal 式输入拍平法来自 Rocktäschel 2015[52]——论文 §1、§3.3
【合理重构】
a. "sentiment neuron 执念"作为动机主线——依据：作者公开研究轨迹的连续性（重构，非论文所载）
b. 时间点 2018 年 3 月、"两件事撞在一起"的触发场景——依据：引文[21][34][44] 均发表于 2018 年初；GLUE[64] 在 idea 期尚未存在（2018.4 发布），但它在成文时已被论文多处引用——§1 第 75 行 the recently introduced GLUE multi-task benchmark [64]、§4.2、Table 4 caption、脚注 1 指向 gluebenchmark.com/leaderboard【可查证：论文全文】。修订注：原稿此条作"（故全文未提 GLUE）"，与论文事实相反（四处出现 GLUE），错误来源是把"idea 期 GLUE 未发表"错推成"全文未提"；本错误还与《档案 · S2》第 5 条直接对撞。详见 S8 第 2 节 H1 与本文件末尾的《S1 勘误》
c. naive 想法的具体形态（LSTM 特征路线）——依据：这是当时该背景下最省事的默认选项
【负结果】
1. naive 想法"LSTM 特征路线"被毙，死因三条：特征路线留任务专用新参数（§2）；LSTM 长程依赖弱+训练慢；分类坑位已被 ULMFiT 占住
（本阶段负结果即为主体内容，占比 >60%）
【未决问题】
1. 预训练语料选什么？（留给 S3/S4）
2. 目标函数没共识，为什么敢押注 LM？（留给 S2/S3）
3. 拍平输入会不会丢结构信息？（留给 S3）
4. Transformer 当 LM 的训练成本是否可承受？（留给 S4）
【后见之明自检】未引用任何实验结果与性能数字；"押注 Transformer"仅以架构特性论证，未断言其必然成功——合规。
```

---

> **【修订注 · S8 核查 · H1】** 原稿此条作"（故全文未提 GLUE）"。**此说与论文事实相反**：论文 §1／§4.2／Table 4 caption／脚注 1 共四处出现 GLUE。原稿的错误来源是把"idea 期 GLUE 未发表"错推成"全文未提"。现按事实改为：idea 期（2018.3）GLUE 未存在，成文期（2018.6）已被引用。另注：本错误与《档案 · S2》第 5 条"GLUE[64]（2018.4）论文引用了"直接对撞，两份档案互相矛盾而未被发现——详见 S8 第 2 节 H1。
>
> **【修订注 · S8 核查 · H7】** 档案第 1 条原标【可查证】，但 sentiment neuron 原文（Radford, Jozefowicz, Sutskever 2017）不在论文参考文献表内，属论文外材料；已在条目内改标【可查证·论文外】，并注明论文 Table 4 里 [16] 的真实身份是 block-sparse kernels。

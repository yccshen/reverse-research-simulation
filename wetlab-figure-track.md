# 图像密集型湿实验论文的仿真改编方案（图轨 / 变体 B）

> 本文件是九阶段流水线的**第三轮产出**，回答一个前两轮没有数据回答的问题：
> **原模板是为"纯文本 + 表格驱动"的语言模型论文设计的，换成一篇 13 页、6 张主图、12 张补图、9 条 Note 的湿实验器件论文，模板要改哪几处？**
>
> 结论先行：**改三处，加一轨，删零处。**
> - 改：**S3 建模迭代 → 工序迭代**；**S4 双轨 → 三轨（加被试与协议轨）**；**S8 第 0 步"重算趋势表" → "逐图重算 + 三方对齐"**。
> - 加：**图轨（figure track）**——一条与《档案》平行的产物，S4 与 S8 的共享输入。
> - 删：无。原模板的五条最高纪律在湿实验论文上全部仍然必要，其中"负结果占比 ≥40%"在湿实验里更容易达成（失败更多）。
>
> 证据基础：第三轮实例 `paper-sim/MI-1/`（Li et al., *Sci. Adv.* 2025, eadv3359，柔性多通道肌电阻抗传感器）。全部读数与脚本可核。

---

## 第 0 节｜先诊断：原模板的三根支柱在湿实验论文里都换了名字

| 原模板的支柱 | 它在语言模型论文里的形态 | 它在湿实验论文里的形态 | 是否可直译 |
|---|---|---|---|
| S3 的"建模迭代" | 算法版本 v0→v1→v2，死因是理论缺陷或实验预期缺陷 | **配方与工序版本**，死因是器件指标（模量、接触阻抗、耐应变、粘附力、均匀性） | ❌ 必须改判 |
| S4 的"数据轨" | 语料从哪来、怎么清洗、规模怎么定 | **被试与协议轨**：几个人、什么动作、多少次、怎么分训练测试、统计怎么做、伦理怎么过 | ❌ 必须改判 |
| S8 第 0 步的"重算趋势表" | 把论文的表格逐行做差值，找概括句的反例 | **逐图重算**：把图上的读数与正文数字对照 | ❌ 但**可以做得更硬**，见第 2 节 |
| S4 的"消融纪律"（归因必须与消融表逐任务差值对齐） | 有消融表就能执行 | **多数湿实验论文没有消融表，只有"条件下并列"** | ⚠️ 降级 + 换判据 |

**前两轮把"图不可核验"记成了一条物理限制。** GPT-2 轮 S8 的原话是：

> Figure 1–5 **全部无法从纯文本核验**（提取只有 caption）。叙事中任何涉及图内形态的断言都只能回溯到 caption 或正文旁证。**这不是叙事的疏漏，是数据源的物理限制。**

**这一轮把这条限制从"限制"改写成了"通道"。** 做法只有一个：**真的去读图**。第三轮的图轨在 6 张主图上产出了 **8 条实质发现**，其中两条直接改变了结论走向（见第 2.4 节的"撤销"清单）。

---

## 第 1 节｜图轨：定义、产物、三条纪律

### 1.1 定义

**图轨是图像密集型论文里与《档案》平行的一条产物。** 它只回答一个问题：**从图上能直接读出什么？** 它不做因果推断、不评价方法好坏、不判断设计是否合理。

定位：
- 它是 **S4 与 S8 的共享输入**（S4 用它取协议类读数：类别数、通道数、面板结构；S8 用它做图-文对照）；
- 它**不是**叙事的一部分，因此**不适用"无后见之明"纪律**（读图必然发生在一切之后）；
- 它**受"来源标注义务"纪律约束**，且标注粒度更细：**必须到"图号 + 面板号"**。

### 1.2 三条纪律（与《档案》的标注义务同构，但更严）

1. **只报可从图上直接读出的量**：轴范围、刻度、平台高度、柱高、条数、曲线端点、是否顶到上限、面板计数。**不做曲线拟合式的定量外推。**
2. **每个读数带"图号 + 面板号"**（等价于【可查证】的图版级出处）。凡目视估算或像素分割得来的，**必须写"约"并给出误差量级**。
3. **读不出的必须写"读不出"或"缺件"，不许用正文叙述补位。**

> 第 3 条是这一轨能不能用的分水岭。第三轮实例里，`_图轨读数.md` 明确登记了两处"不可读"：Fig. 3D 横轴末端的确切通道数（裁剪后未见末端刻度）、Fig. 6C/6D 的宽度与深度色带（**色标没有独立纵轴，绝对读数不可得**）。**宁可留白，不可补位。**

### 1.3 工具链（本轮固化的四步）

```powershell
$py = "C:\Users\A\.workbuddy\binaries\python\envs\default\Scripts\python.exe"
# ① 抽图：内嵌栅格 + 整页渲染（两条都要）
& $py _extract_figs.py <paper.pdf> figs
# ② 裁剪放大：按分数框裁一个面板，用于精读刻度与标注
& $py _crop.py figs\figN.png figs\crop_NC.png 0.10 0.30 0.55 0.68 2.2
# ③ 定位框线：找长直线，得到绘图区像素边界
& $py _findframe.py figs\figN.png 0.30 150
# ④ 曲线数字化：颜色分割 + 用图内自带时间戳做标定
& $py _digitize6.py figs\figN.png
& $py _calib6.py  figs\figN.png
```

**四个能省很多时间的经验**：

1. **优先用内嵌栅格，不要只用整页渲染。** 很多期刊的"矢量图"实际是**一张整幅内嵌位图**（第三轮 6 张主图全部如此，分辨率 1838×1547 至 2167×2072）。拿到原始栅格后，裁剪放大比整页渲染清楚一个量级。
2. **裁剪要看刻度标签，不要只看曲线。** 第三轮两处最重要的发现（Fig. 6C/D 的**不同纵轴量程**、纵轴零点在**刻度线而非边框**上）都是靠单独裁一条纵轴标签带才读出来的。
3. **找框线时阈值要调松，并且排除插图区。** 器件论文的 Fig. 1 常有大幅渲染图，会把"长直线"检测淹掉。把扫描范围限制在**右侧三分之二**并用 coverage>0.45 的松阈值，能稳定拿到成对的横框线（第三轮拿到 `190/219、414/441、638` 这一组，正好对应 B/C/D 三个面板的底与顶）。
4. **标定不要靠推测轴范围，要找图内自带的标定点。** 第三轮最漂亮的一步：Fig. 6B 里有 **5 条灰色竖虚线**，而图注写明"5 个时间点对应 (B) 中的灰色虚线"并给出 5.3 / 14.5 / 23.1 / 42.2 / 56.2 秒。**用这 5 个时间戳反解像素-秒比例（18.5 px/s）**，比目视估轴范围准，而且**自带残差检验**（最大残差 0.6 s，出现在 14.5 s 标点上）。同一技巧适用于任何"图注给了离散时间点 + 图里有竖虚线/标记"的论文。

---

## 第 2 节｜三方对齐审计：正文 × 图注 × 图面

### 2.1 为什么必须做三方而不是两方

文本里有两类不同的"说法"：

- **正文的数字**——它可能来自图，也可能来自缺件；
- **图注的描述**——它描述的是**图上画了什么**，因此它是最接近图面的文本锚点。

**正文可以隔着一层（引用缺件），图注不能。**所以当"正文 vs 图面"出现不一致时，**先看图注站在哪边**，这一步能立刻分开两种截然不同的情况：

| 情况 | 判据 | 定级方向 |
|---|---|---|
| 图注与图面一致、正文与图注不一致 | 图上确实如注所述 | **正文口径问题**（措辞或选边） |
| 图注与正文一致、两者与图面都不一致 | 两处文本共同偏离图面 | **图面或数据问题**（严重度更高） |
| 三者互不一致 | 逐条核，必要时用像素级读数裁定 | 逐条定级 |

### 2.2 第三轮用这一方法抓到的五类不一致（可直接作为模板）

| 类型 | 第三轮的实例 | 为什么只有读图能抓到 |
|---|---|---|
| **对照物掉出图外** | Fig. 2D 中商用 TPU 胶带一支在第 4、5 天**图上无柱无散点**，而正文只报自己 13.57 → 10.55 N/m，一字未提对照物消失 | 正文与图注都"正确"，**缺席发生在图面上** |
| **两个面板量纲不同** | Fig. 6C 纵轴 0–60 mm、6D 为 0–40 mm；同一组对照用不同量程后两条曲线视觉上等高 | 正文只说"趋势一致"，**只有把两个纵轴并排看才发现尺度不可比** |
| **幅值系统性偏差** | Fig. 6C/D 数字化后：VR 估计的切割长度全程高于实拍值，**端点 1.50 倍、首个平台处最高 2.6 倍**；而正文把差异**全部**归给宽度与深度 | 需要曲线数字化才能给出倍率 |
| **单位与量的错配** | Fig. 2F 与 2G 的纵轴标注为 `Impedance (mV)`——mV 是电压单位 | 正文解释了"恒流源下电压正比于阻抗"，**轴标签的错误只在图面上** |
| **基线未对齐** | Fig. 5E 的两项疲劳指标在两个条件下**基线方向相反的偏移**：MNF 的致动组起点高约 10.5 Hz，MDF 的致动组起点低约 4 Hz；正文与图注均未声明 | 需要逐点读数才能给出偏移量；**而两项指标同时出现反向偏移这一"模式"，只有把两张小图放在一起读才浮现** |

### 2.3 反向核验：图轨也能**证实**文本

图轨不是只用来找茬的。第三轮有两处**正向核验**，其中一处直接把一条本来会被判成 P0 的矛盾降级：

- **混淆矩阵的直接复现**：Fig. 3C 是 10×10 行归一化矩阵。**逐行求和**（10 行全部等于 100，无残差）后取**对角线均值 = 98.22%**，与正文"participant 1 的总体准确率 98.22%"**逐位吻合**。于是一处"摘要写 98.49%、正文写 98.22%，是不是笔误"的疑问被解决：**98.49% 是三人均值，98.22% 是单人值，两者不是矛盾而是口径切换。**
- **分布形态的自洽**：Fig. 3D 中 Chinese seven / Thumb up / Wrist dev 三行的重复测量带离散度明显大于其余七行，与混淆矩阵里这三类互认错（91.1 / 94.1 / 对角线下方的 7.9 与 5.9）方向一致。

> **这条经验值得单独记**：**凡论文里同时出现"两个都合理但不同的数字"，先去看有没有一张图能直接算出其中一个。** 能算出，就是口径问题；算不出，才升级为矛盾。第三轮靠这一步把一条候选 P0 降成了 P2。

### 2.4 图轨最重要的用途：**撤销假发现**

这是本轮最出乎意料的一条。**像素级读数两次驳回了"看起来很明显"的判断**：

| 被撤销的假发现 | 提出者 | 撤销依据 |
|---|---|---|
| "Fig. 5C 与 Fig. 6B 的归一化预测力顶到 100% 上限，说明回归输出被截断" | 主线叙事（读图前的先验猜测） | Fig. 5C 实测读数：预测力峰值只有 **45%–52%**，离上限差一半。**饱和只发生在 Fig. 6B，不发生在 Fig. 5C。** 该假发现被撤到只剩"6B 单面板"这一条 |
| "Fig. 5Ci 激活段的 EMG 幅度明显大于未激活段，与正文'肌肉激活水平相当'不符" | **隔离审稿人（MW3）** | 颜色分割逐列测幅：非致动区 `mean max|dev| = 20.9 px（p95 52, max 73）`，致动区 `20.4 px（p95 47, max 70）`。**两区幅度实质相同，非致动区甚至略高。审稿人的指控不成立**，正文该句得到反证 |

**结论（这是本轮对模板最有价值的一条）**：
**图轨的价值不止于"发现论文的问题"，也在于"阻止流程自己制造问题"。** 一个只会找茬的审计通道，会把"看起来不对劲"升级成指控；只有可量化的读数才能同时**否决**这些指控。**因此图轨必须与叙事和审稿输出同权，而不是作为它们的附属。**

---

## 第 3 节｜缺件清单：把"不可核验"从"覆盖度缺口"里分出来

### 3.1 三类产物必须分开记

前两轮的 S8 只有两类：**【对上了】**与**【编圆了但论文没这回事】**，外加一节【论文有而叙事漏了】。**湿实验论文需要第三类**：

| 类别 | 定义 | 第三轮的处理 |
|---|---|---|
| 覆盖度缺口 | 论文里有、叙事没吸收 | 沿用前两轮，逐条列 |
| **不可核验清单** | **论文的断言依赖本流程拿不到的图/表/Note/影片** | **本轮新增，单列一节** |
| 论文自身问题 | 论文文本层面的硬冲突或不一致 | 沿用前两轮 |

**为什么必须分开**：覆盖度缺口是**叙事的失分**（可以改叙事来补）；不可核验是**数据源的边界**（改叙事补不了，只能声明）。混在一起会让读者以为"多写点就能补齐"。

### 3.2 第三轮的缺件规模（给一个量级感）

论文 13 页正文，**6 张主图**；补充材料 **9 条 Note + 2 张表 + 12 张补图 + 4 段影片**，全部不在手上。

把它折算成一句可比较的话：

> **主图与补图的比例是 6 比 12；主文里没有任何数值表格。**

更硬的一个度量（**这一句按第三轮 S8 的复核结果写，初稿曾把它说得过头**）：**论文的 8 组核心问题中，只有 1 组（"FMEIS 相对肌电的增量"）的完整证据完全在位；另有 3 组的关键数字（模量、深度、三人均值）其原始数据在缺件里；其余 4 组主图在位但补充证据与影片缺件。**

**初稿写的是"7 组都至少有一半落在缺件里"——那是把"补充证据缺件"与"主要证据缺件"混为一谈**（比如"贴得住吗"一组的四项证据里，有三项就在正文的 Fig. 2B/C/D 里）。**这个错法本身值得记下来：统计不可核验的比例时，必须按"组"和"关键数字"两级分开算，否则会系统性地高估缺件的严重程度。**

**并且缺的东西恰好是最关键的那一块**：主文里唯一一张做定量对照的图（六轴雷达图 Fig. 1G）**没有数值标尺**，其判据与数据被图注指向 `table S2`，而 `table S2` 缺件。

### 3.3 主/补分工的一条可复现规律

把"这条内容被放在主文还是补充"逐条登记后，规律是清楚的：

> **凡是"概括后的正面结论"留在主文；凡是"原始数据"与"不利的那一半"移出主文。**

第三轮的实例：
- **"低模量"这个核心卖点的原始拉伸数据** → fig. S2（补）；
- **走线电阻随应变的变化**（同时含"50% 内电阻仍低"与"30% 出现裂纹"两面）→ fig. S1（补），**主文只引正面的一半**；
- **"可复用"的证据（8 小时、5 天）** → fig. S5（补）；
- **深度的唯一证据** → fig. S6（补）；
- **三人均值的原始数据** → fig. S10（补）。

**这不是不当行为**（13 页确实放不下 18 张图），**但它的后果可以被精确度量，因而必须被写下来**：主文的可核验度被系统性地压低了。**审计方的职责是把这件事量化，而不是把它写成道德判断。**

---

## 第 4 节｜S3 改判：工序迭代

### 4.1 改判依据

湿实验论文的迭代对象通常**不是算法，是配方与工序**。第三轮论文的 Materials and Methods 里，器件工艺参数给到**配比级**（每 10 克 PDMS 加 30 微升 80% PEIE 溶液；AAc 占 AAm 15 重量百分比；PEGDA 1%、NaCl 5%、TPO 0.5%；AAm 与水 0.35 比 0.65；搅拌 8 小时；银浆 110 摄氏度 40 分钟；封装 90 摄氏度 1 小时）——**这些不是"算法超参"，它们是"配方"。**

### 4.2 S3 的新增必备产物：**"有动机句 × 有引文"四象限表**

这是本轮加进 S3 的最有用的一个动作。做法：**把论文里每一个设计要素列出来，只问两个是非题**——① 论文有没有一句话说明"为什么选它"（动机句）？② 论文有没有引文？

|  | 有引文 | 无引文 |
|---|---|---|
| **有动机句** | 两者都有（可信度最高） | 有动机无引用（作者自证） |
| **无动机句** | 有引用无动机（**引了但没说为什么——本轮新增条款的直接产物**） | **两者都无（决定性的黑洞，必须逐条登记）** |

**第三轮的结果**：PEIE 改性、双网络水凝胶两项属"两者都有"；NaCl 属"有动机无引用"；**走线材料、走线条数（16）、总厚 220 微米、刷涂封装、K-705 密封五项属"两者都无"**。

**而这张表最有价值的读法是看分布**：**有动机的三项都在材料化学层，无动机的五项都在器件结构层。** 这句话是读论文读不出来的，只能靠这张表读出来。

### 4.3 死因判据换掉

S3 模板原文的判据是"**每一版的死因必须说得让内行信服**"（理论缺陷或实验预期缺陷）。**湿实验里要换成器件指标的三种属性**：

| 指标属性 | 判据形态 | 第三轮实例 |
|---|---|---|
| **越大越好** | 单调，可比较 | 粘附力、接触阻抗的余量 |
| **有可用窗口** | **不是单调最优** | 粘附力（太大了撕下会疼，太小了会滑） |
| **越小越好** | 单调 | 模量与皮肤的偏差、总厚度 |

> **第三轮最实用的一条经验来自这里**：论文从 Ref 32 借来了"医用胶带 15.75–110 N/m"这个区间做对照，而**自研粘附力在 8 小时后掉到 9.8 N/m，跌穿了该区间下限**。项目能继续，靠的是把判据从"比胶带强"（绝对量）换成"**是否够用**"（有无窗口）。
> **教训：如果你拿别人的量纲当自己的门限，就必须先标清楚这个指标的属性。指标属性没标，你借来的标尺最后会回来量你。**

---

## 第 5 节｜S4 改判：三轨

### 5.1 新轨：被试与协议轨（替代"数据轨"）

要问的问题清单（**每一项在多数湿实验论文里都会缺一两项，缺哪项本身就是结论**）：

1. **几个人？怎么招的？** 性别、年龄、体格（身高体重、**体脂或脂肪厚度**——因为论文常把"对个体差异稳健"作为 claim）
2. **是不是同一批人做了全部实验？**（器件表征、算法验证、应用演示分别用了谁）
3. **协议参数**：激励量、测量配置、采样/更新率、单次时长、重复次数、任务集
4. **训练与测试怎么分**：比例、是否交叉验证、**特征选择发生在哪个集合上（信息泄漏风险）**
5. **统计**：用了什么检验、**自由度**、**是否多重比较校正**、效应量与置信区间、**`±` 是样本标准差还是总体标准差**
6. **伦理**：批号、依据的宣言、是否有额外风险论证

### 5.2 一个必须新增的算子：**从协议参数换算出运营指标**

论文通常只给"采集耗时"，不给"样本量"。**而样本量决定了读者能不能判断模型容量是否合理。**

第三轮的换算：
- 帧周期 **127 毫秒**，10 个手势训练采集 **2.2 分钟** → **1039 帧 / 10 类 ≈ 每类 104 帧**
- 力任务 **90 秒** → **709 帧 / 7 档 ≈ 每档 101 帧**

**一个 120 维输入、10 类输出的任务配一个 25 节点的单隐藏层网络**——把这两个数放在一起，读者才知道 25 这个容量是**被样本量约束住的**，而不是设计偏好。**论文一个数都没给，但两个数都是现成的。**

同类换算还要做：**周期数**（"13 分钟、每 10 秒交替 → 39 个完整周期，论文报 38 个，差 1 属边界效应"）、**组合数**（"16 个电极的全部两两配对 = 120，与论文的'每帧 120 个独立阻抗值'数值相同"——**注意：这一步是推断，验证需要缺件的 Note S5，按措辞强度条款只能写"可能是"**）。

### 5.3 两个必查的口径滑动（湿实验特有）

1. **样本量与 claim 的不匹配**：3 名受试者 → "strong robustness against variations in skin impedance and adipose tissue thickness"。**而脂肪厚度既没测也没报**，却出现在被解释的变量列表里。
2. **"均值 ± 标准差"的读者默认**：本论文的 ANOVA / F 检验**全部用在特征选择上，从未用于组间推断**。但报告里出现"均值 ± 标准差"，读者会自动读成"经过统计检验的结论"。**这个区别在论文叙述里没有被点明。** 湿实验论文里这一类口径滑动极其常见。

### 5.4 消融纪律的降级判据（本轮最重要的负结果之一）

原纪律："凡'某组件救了我'的归因，必须与该组件在论文消融表里的逐任务差值对齐。"

**湿实验论文里多半没有消融表，只有"条件下并列"。两者的区别必须写清**：

- **消融**回答"我的哪个组件在起作用"——需要**把一个设计拆掉再测**；
- **条件对照**回答"我在不同条件下表现如何"——需要的是**工况切换**。

**第三轮的判定**：论文有四处条件对照（水凝胶 vs PDMS、FMEIS vs 肌电、未致动 vs 致动的外骨骼、三种最大握力条件），**而一次真正的消融都没有**。于是论文的核心因果链——"因为超薄、低模量、高粘附，所以运动伪影低，所以精度高"——**在论文内部从未被隔离验证过**。

> **一句可直接复用的判据**：**并列陈述 + 无条件对照 = 一条未被验证的因果链被读成了已验证的因果链。**

---

## 第 6 节｜S5 改写：湿实验的至暗时刻类型学

前两轮的至暗时刻模板（"实验崩了：现象、排查路径、根因"）在湿实验里要按**失败类型**组织。第三轮归纳出四型，可作为 checklist：

| 型 | 形态 | 第三轮实例 | 能否在 S1 避开 |
|---|---|---|---|
| **一｜看不见信号** | 不知道是"量不存在"还是"器件不行"，而两者处置完全相反 | 三周里阻抗曲线只有与漂移同量级的波动 | ❌ 不能。只能靠仿真或实验把分叉剪开 |
| **二｜指标判据失效** | 借来的对照标尺反过来卡住自己 | 8 小时后粘附力跌穿医用胶带区间下限 | ⚠️ 部分能（见第 4.3 节） |
| **三｜设计前提崩塌** | 不是实现问题，是方案前提的问题 | 换一片贴片，模型基本等于随机 | ✅ **能，而且必须能** |
| **四｜训练工况与使用工况错位** | 模型没有输入能察觉工况变了 | 虚拟手术里手腕有三个自由度，而模型只见过"捏合" | ⚠️ 能预判，不能避开 |

**型三的根因值得单列成一条通用判据**（本轮最有价值的一条经验）：

> **凡特征的定义里含有一个"不可控的物理位置或几何量"，该特征就自带一个泛化边界；这个边界必须在方案阶段被写下来。**
>
> 第三轮的链条：S1 为了避开病态的逆问题，把"16 个电极上的电压变化向量"当特征（而不是去重建电导率图像）。**这个决策救活了项目，同一个决策也埋了刀**——向量的每一个分量都是"某个固定电极位置看到的电场"，几何被编码进特征的定义里；位置一变，特征的含义就变。
> **如果在 S1 多问一句"这个向量的定义里绑定了哪些不稳定的量"，型三就从一个砸下来的意外，变成一个被标注的已知风险。**

**并用一处"仿真乐观"的量化痕迹收尾**：仿真给出最大检出深度约 **35 毫米**，实测约 **30 毫米**（相对差 14%），论文表述为"紧密吻合"。**仿真能做的是"排除不存在"，不能做的是"证明存在"。**

---

## 第 7 节｜S7 改写：湿实验期刊审稿人的三条标配攻击

隔离审稿人（第三轮实测）的三条主要弱点全部落在**三类标配攻击**上，可作为提示词的检查清单：

1. **机理归因不唯一**（对照不足 / 混杂 / 串扰）。本轮 MW1：**"腕伸展时 FCR 的阻抗变化不能唯一归因于 FCR 被动牵拉，也可能含拮抗肌 ECR 主动收缩的串扰"**——而且审稿人**用论文自己的 Figure 2E 做论据**（单肌收缩在体表产生的是**有符号的广域空间模式**，所以串扰是真实存在的机制）。**这一类攻击的杀伤力最大，因为它不需要新实验就能成立。**
2. **样本量与验证方式不足**（跨被试、交叉验证、误差分析）。
3. **系统级指标达不到应用水位**（延迟、更新率、对照缺失、图文不一致）。

### 两条实操经验

- **rebuttal 里"部分据理力争"的据，最好取自图内已有的内部对照。** 第三轮 MW1 的反驳用的是 **Fig. 2G 里 FCR 与 ECR 两条曲线的反相结构**：若串扰主导，两条通道应当同相；**反相结构的存在说明局部肌肉贡献占主导**（串扰改变的是反相幅度，不是制造反相结构）。**这是"用论文自己的图当作内部对照"的典型用法，同一篇论文里已经存在、成本为零。**
- **落地对照表要统计"承诺的类型分布"，不要只统计落地率。** 第三轮：11 条承诺中完全落地 2 条、部分落地 2 条、**未落地 7 条——而 7 条里有 5 条属于"加一句限制说明"这种成本极低的改动**。
  > **低成本的承诺未落地，比高成本的承诺未落地更值得记录**，因为它不能用"改不动"解释。**但注意纪律：只登记统计，不给归因**——篇幅让位、主动削弱结论、期刊流程不强制逐条回应，三种可能在本流程内无法区分。

---

## 第 8 节｜S8 改写：第 0 步的新框架

### 8.1 框架的替换

| | 语言模型论文 | 湿实验论文 |
|---|---|---|
| 第 0 步的框架 | "**逐行算差值**" | "**逐图逐行重算 + 图注-正文-图面三方对齐**" |
| 审计物 | 表格 | 图 |
| 可做的算术 | 求和、平均、差值、比例 | 同上 **+ 像素级读数、曲线数字化、量纲对齐** |

**观察框架必须与 S7 明确不同，且两方互不引用**（GPT-2 轮固化条款，第三轮继续执行）。两份提示词可以**共用同一份"图件路径清单"**——否则两者对图的处理方式不同，发现集合就无法比较。

### 8.2 审计员的禁令（照抄，不许放宽）

- **只做算术与文本/图面对照**；
- **不得评价方法好坏、不得说"这个设计有问题"、不得提任何改进建议、不得推测作者意图或研究过程**；
- **读不出来的必须写"读不出"**，不许用正文叙述补位；
- **不给出"总体评价"段落，只给清单**。

第三轮实测：遵守禁令的审计员产出的是**五节清单**（概括句与其支撑物 / 文内算术核对 / 图-文-图注三方对照 / 不可核验清单 / 我没能判定的）。

### 8.3 S8 新增的必核项（本轮从实例里抽出来的）

1. **两个隔离视角的发现集合是否仍然不重合**（GPT-2 轮为 2/11）。第三轮审稿人独有：机理归因不唯一、离散力档可能是分级。图轨独有：对照物掉出图外、两面板量程不同、1.5–2.6 倍幅值偏差、单位错配、基线反向偏移。**两者都漏的由本阶段补。**
2. **对隔离审稿人自身做一次失准核查**（沿用 GPT-2 轮做法）。第三轮查出 **三处**：误称某图缺对照（实际有）、其切割计数与图面不符、把一处数字的出处标错位置。
3. **假发现的撤销清单**（本轮新增）。**把被图轨或像素读数驳回过的一切记下来**，包括流程自己提出的和被审稿人提出的。**这一节是审计报告里最能体现"审计通道是双向的"的部分。**

---

## 第 9 节｜S9 改写：给外行讲一篇湿实验论文

### 9.1 五幕 / 六幕的判据不变，但"讲法"那一幕的论点形态不同

判据仍是 GPT-2 轮固化的那条：**看"讲法"这一幕有没有独立论点**。

第三轮的独立论点形态与前两轮不同：
- GPT-2 的讲法论点 = **"能改的只有说法，而每一个说法上的决定都是防守。"**
- 第三轮的讲法论点 = **"主文与补充的分工不是详略，是选边——结论放主文，原始数据与不利的那一半放补充；摘要再做一次选边。"**

**依据都是逐条可查的清单**（主/补位置清单 + 摘要藏起清单），因此都够格单列一幕。

### 9.2 湿实验 S9 的三个特有点

1. **实物图与照片无法用文字替代，但必须被"翻译"成物理量。** 给外行的类比要落在**手感**上（"像一张特别薄的创可贴，撕下来不疼，贴回去还牢"），而不是落在参数上。**每个类比后面仍必须补一句"它哪里不像"。**
2. **"藏起"清单要按失利条目写，不按数字写。** 第三轮的四项是：换贴片即需重训练 / 复杂动作下力估计错位 / 应变 30% 出现裂纹 / 深度只有 30 毫米。**写"藏起了三项失利 + 一处口径切换"比写"藏起了一个数字"信息量大得多**（这条沿用 GPT-1 轮定稿时确立的口径）。
3. **外行闸的抽检三段要覆盖不同难点类型**（GPT-2 轮固化）。湿实验论文的难点类型与语言模型论文不同，抽检组合建议为：
   - **一张实物图/工艺步骤的说明段**（"这段讲了什么工序"）
   - **一组数值比较段**（"这两条曲线的差别在哪"）
   - **一段机理类比段**（"它为什么能测到肌电测不到的东西"）
   并且**必须告诉被抽检者：答错说明写作者写得不够外行友好，请如实说"没看懂"**。

---

## 第 10 节｜可直接执行的清单（Checklist）

### 拿到一篇图像密集型湿实验论文时

- [ ] **S0**：主图是否全部在位？**"支撑核心 claim 的定量对照"是否在位**？（第三轮的答案是"图在、表缺"，这个组合最危险）
- [ ] **S0**：缺件清单（Note / Table / Fig / Movie 各几条）先列出来，作为全流程的核验边界
- [ ] **S0**：有无被试与伦理组件？有无仿-实一致性环节？有无器件指标的对照物？
- [ ] **图轨**：① 抽内嵌栅格 + 整页渲染 ② 逐面板裁剪放大（**含纵轴标签带**）③ 找框线 ④ 曲线数字化（用图内自带标定点）
- [ ] **图轨**：如实登记"读不出"与"缺件"，不许用正文补位
- [ ] **S3**：改用工序迭代；产出"有动机句 × 有引文"四象限表；把每个器件指标标上属性（越大越好 / 有窗口 / 越小越好）
- [ ] **S4**：三轨（被试与协议 / 器件与性能 / 图轨）；**从协议参数换算出样本量与周期数**；查两处口径滑动（样本量 vs claim、均值±标准差 vs 统计推断）
- [ ] **S4**：确认是消融还是条件对照；把"并列陈述 + 无条件对照"写成一条负结果
- [ ] **S5**：按四型失败组织至暗时刻；**逐型回答"能否在 S1 避开"**
- [ ] **S7**：隔离审稿人用三条标配攻击做检查清单；rebuttal 优先取图内已有的内部对照；落地对照表统计"承诺类型分布"
- [ ] **S8**：第 0 步框架换成"逐图重算 + 三方对齐"；审计物改为图；单列**不可核验清单**；单列**假发现撤销清单**；对隔离审稿人做失准核查
- [ ] **S8**：统计主/补分工的规律，并度量"主文每条 claim 对应几张不可见的补图"
- [ ] **S9**：判断讲法幕能否单列；外行闸抽三类不同难点

---

## 第 11 节｜本方案的边界（诚实声明）

1. **样本量仍然只有 n=3**，且三篇论文分属**两种形态**（两篇语言模型技术报告 + 一篇湿实验期刊论文）。**"湿实验"内部还有大量子类型没有数据**：化学合成类、生物学体内实验类、临床研究类、材料表征类——它们的图轨形态与缺件结构都可能不同。
2. **图轨依赖模型的多模态读图能力。** 本轮全部读数由人（模型）目视 + 脚本像素分割完成，**没有使用任何曲线数字化专用库**。对坐标轴密集、曲线互相遮挡、或只有低分辨率截图的情况，本方案的可靠性没有数据。
3. **"不可核验清单"的比例会随论文形态剧烈变化**。本轮的比例（8 组核心问题 7 组沾缺件）**可能是高的**，因为这是一篇 13 页正刊短文配 12 张补图 + 9 条 Note 的版式。**预印本、学位论文、会议长文的缺件结构完全不同。**
4. **图轨能撤销假发现这一条，只证明了"它可以撤销"，没有证明"它能撤销足够多"。** 本轮撤销 2 条、证实 2 条、发现 8 条——**这个比例是单例，不是基线。**
